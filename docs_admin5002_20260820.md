# 后台管理独立通道（2026-08-20, S8, 任务 T-1787237745）

## 目的
后台管理（/api/admin*、/api/admin_v2*）请求原先与生产业务（设备轮询/小程序/微信回调）
共用 5001 的 8 个 gunicorn worker，高峰期互相抢资源导致后台加载卡顿（"转圈"）。
方案A：单独起 2 个 worker 监听 5002，nginx 把后台管理请求分流过去，生产业务完全不受影响。

## 改动清单
1. 新增 /etc/systemd/system/smart-locker-admin.service
   - gunicorn 2 worker, bind 127.0.0.1:5002, 同 app:app
   - 无 ExecStartPre 杀端口（避免与主服务冲突）
2. 改 /etc/nginx/sites-enabled/locker-cqdyxl
   - 新增 location ^~ /api/admin/ → 127.0.0.1:5002
   - 新增 location ^~ /api/admin_v2/ → 127.0.0.1:5002
   - 其余 /api/ 仍走 5001（生产业务不变）
3. 改 /etc/systemd/system/smart-locker.service
   - ExecStartPre 从 'fuser -k 5001/tcp 5002/tcp' 改为只杀 5001/tcp
   - 避免主服务下次重启时误杀 5002 的 worker

## 验证
- 5002 独立健康 200
- nginx -t 通过，reload 平滑
- /api/admin/dashboard 经 nginx 后与直接访问 5002 响应一致（401 未登录）→ 分流成功
- /api/locations 经 nginx 后与直接访问 5001 响应一致 → 业务仍走 5001
- 设备轮询日志正常（nginx access log 显示 okhttp 设备请求照常）

## 备份
- /etc/nginx/sites-enabled/locker-cqdyxl.bak_20260820_admin5002
- /etc/systemd/system/smart-locker.service.bak_20260820_admin5002
- （smart-locker-admin.service 为新建，无需备份）

## 回退
- 注释 nginx 两处 location ^~ /api/admin* 块 → nginx -t → reload → 后台回到 5001
- sudo systemctl stop smart-locker-admin → 5002 下线
